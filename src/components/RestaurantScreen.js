import RestaurantList from './RestaurantList';

const RestaurantScreen = () => {
  return (
    <div>
      <h1>Restaurants</h1>
      <RestaurantList />
    </div>
  );
};

export default RestaurantScreen;
